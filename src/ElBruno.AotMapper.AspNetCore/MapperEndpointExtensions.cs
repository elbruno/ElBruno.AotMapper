namespace ElBruno.AotMapper.AspNetCore;

/// <summary>
/// Convenience extensions for mapping in ASP.NET Core endpoint handlers.
/// These are thin wrappers that make the generated mapping methods more discoverable in API contexts.
/// </summary>
public static class MapperEndpointExtensions
{
    // Placeholder — generated extension methods (ToXxx) are already usable directly.
    // This class provides any ASP.NET Core-specific mapping utilities.
}
